/* Supabase configuration for the AI Research Showcase.
 * Project URL was recovered from the ADS Phoenix licensing sessions.
 * The ANON key is client-safe (public by design; Row-Level-Security protects data)
 * — paste it below to ACTIVATE the login gate. While it stays as the placeholder,
 * the gate is OFF so local preview keeps working. The service_role key must NEVER
 * go in this file. */
window.SB_CONFIG = {
  url:  "https://cmrkzplddkdmyjxlsazx.supabase.co",
  anon: "PASTE_YOUR_SUPABASE_ANON_KEY_HERE",
  // Optional: restrict sign-ups/logins to your domain (auto-allow). Others can be
  // approved in Supabase. Leave empty to allow any authenticated user.
  allowedDomain: "asure.in"
};
